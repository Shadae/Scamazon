debug: <%= params.inspect %>
<%= form_tag(products_path, method: :get) %>
<% Category.all.each_with_index do |category| %>
  <%= label_tag category.category %>
  <%= check_box_tag(:"category_filter[]", category.category) %><br>
<% end %>
 <%= submit_tag 'Filter by categories' %>