

<table>
  <thead>
    <tr>
      <th>Category </th>
    </tr>
  </thead>

  <tbody>
    <% @category.each do |category| %>
      <tr>
        <td><%= category.category %> 
          <%= link_to 'edit category', "/categories/#{category.id}/edit" %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to 'New Category', new_category_path %><br><br>